
<%= render "shared/flash" %>
<div class="container">
<div class="row">
  <div class="col-12 col-sm-3">
    <%= render "shared/sidebar_organizer" %>
  </div>
  
  <div class="col-12 col-sm-9 content">
    <div class="page-title">
      <h1>カード情報確認・変更</h1>
    </div>
    <% if @card.present? %>
    <div class="pay-form">
      <div class="pay-form__notice">
        <p>お支払いは以下に登録されたカードで行われます。</p>
      </div>
      <div class="pay-form__box-card box-wrap">
        <h2><i class="fa fa-address-book-o" aria-hidden="true"></i> 現在登録されているカード</h2>
        <table>
          <tr>
            <th>カード番号</th>
            <td>**** **** **** <%= @card_information.last4 %></td>
          </tr>
          <tr>
            <th>有効期限</th>
            <td><%= @card_information.exp_month.to_s %>/<%= @card_information.exp_year.to_s.slice(2,3) %></td>
          </tr>
          <tr>
            <th>ブランド</th>
            <td><%= @card_information.brand %></td>
          </tr>
        </table>
      </div>
      <div class="pay-form__notice">
        <p><%= link_to "登録されているカードを変更する", user_pays_edit_card_path, class:"submit-btn btn-hover blue-btn" %></p>
      </div>
    </div>
    <% else %>
    <div class="pay-form__notice">
      <p>まだカードは登録されていません。<%= link_to "カード登録", new_pay_path, data: {"turbolinks" => false}  %>よりクレジットカードを登録してください。</p>
      <p>カードを登録することで、イベントの参加がよりスムーズになります！</p>
    </div>
    <% end %>

  </div>
</div>
</div>